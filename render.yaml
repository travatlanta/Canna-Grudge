services:
  - type: web
    name: cannagrudge-api
    runtime: python
    runtimeVersion: "3.11"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn server:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
